---
phase: 04-production-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma.config.ts
  - lib/db.ts
  - package.json
  - prisma/migrations/
autonomous: true
requirements: [INFRA-01]
must_haves:
  truths:
    - "Prisma schema declares postgresql provider"
    - "lib/db.ts uses pg adapter when DATABASE_URL starts with postgres://"
    - "lib/db.ts uses better-sqlite3 adapter when DATABASE_URL starts with file:"
    - "prisma migrate dev creates a migration folder successfully"
    - "Application builds without errors after migration"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "PostgreSQL-compatible schema"
      contains: 'provider = "postgresql"'
    - path: "lib/db.ts"
      provides: "Dual-adapter Prisma client (SQLite for dev, Pg for prod)"
      exports: ["prisma"]
    - path: "prisma/migrations/"
      provides: "Initial migration for Postgres"
  key_links:
    - from: "lib/db.ts"
      to: "@prisma/adapter-pg"
      via: "conditional import when DATABASE_URL is postgres"
      pattern: "PrismaPg|adapter-pg"
    - from: "lib/db.ts"
      to: "@prisma/adapter-better-sqlite3"
      via: "conditional import when DATABASE_URL is file:"
      pattern: "PrismaBetterSqlite3|adapter-better-sqlite3"
---

<objective>
Migrate Prisma from SQLite-only to dual-mode: PostgreSQL for production (Vercel Postgres) and SQLite for local development.

Purpose: INFRA-01 requires the app to run with Vercel Postgres in production. The current setup hardcodes the better-sqlite3 adapter. This plan switches the schema provider to postgresql, installs the pg adapter, and makes lib/db.ts detect the DATABASE_URL scheme to select the correct adapter at runtime.

Output: PostgreSQL-compatible Prisma schema, dual-adapter db.ts, initial migration, updated dependencies.
</objective>

<execution_context>
@/Users/victorroque/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victorroque/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-production-infrastructure/04-CONTEXT.md

@prisma/schema.prisma
@lib/db.ts
@prisma.config.ts
@package.json

<interfaces>
<!-- Current db.ts exports consumed by every API route -->
From lib/db.ts:
  export const prisma: PrismaClient

<!-- All API routes import prisma like this -->
From app/api/sync/route.ts:
  import { prisma } from '@/lib/db'

<!-- Current schema uses sqlite provider — must become postgresql -->
From prisma/schema.prisma:
  datasource db {
    provider = "sqlite"
  }

<!-- prisma.config.ts controls datasource URL at runtime (Prisma 7 pattern) -->
From prisma.config.ts:
  export default defineConfig({
    datasource: { url: process.env.DATABASE_URL || "file:./dev.db" }
  })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pg adapter and switch schema to postgresql</name>
  <files>package.json, prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (current schema with sqlite provider)
    - package.json (current deps — has @prisma/adapter-better-sqlite3, better-sqlite3)
  </read_first>
  <action>
    1. Install production Postgres packages: run npm install @prisma/adapter-pg pg and npm install -D @types/pg. Keep @prisma/adapter-better-sqlite3 and better-sqlite3 — they are needed for local dev (per D-02).

    2. In prisma/schema.prisma, change the datasource block provider from "sqlite" to "postgresql". Remove the comment about switching providers. The datasource URL is controlled by prisma.config.ts at runtime (Prisma 7 pattern), so the schema just declares the provider.

    3. SQLite-specific syntax check: review the schema for any SQLite-only features. The current schema uses @default(cuid()), @default(now()), @updatedAt, String, Int, Boolean, DateTime — all compatible with PostgreSQL. No changes needed to model definitions.

    Note: Do NOT remove better-sqlite3 or its adapter from dependencies — local dev still uses SQLite per D-02.
  </action>
  <verify>
    <automated>cd /Users/victorroque/Documents/Claude-Code/shipoffers-tracking-report && grep -q 'provider = "postgresql"' prisma/schema.prisma && node -e "require('@prisma/adapter-pg')" && echo "PASS"</automated>
  </verify>
  <done>prisma/schema.prisma declares postgresql provider. @prisma/adapter-pg and pg are in package.json dependencies. @prisma/adapter-better-sqlite3 remains for dev.</done>
</task>

<task type="auto">
  <name>Task 2: Update lib/db.ts for dual-adapter mode and create migration</name>
  <files>lib/db.ts, prisma.config.ts</files>
  <read_first>
    - lib/db.ts (current SQLite-only adapter setup)
    - prisma.config.ts (Prisma 7 defineConfig with datasource URL)
  </read_first>
  <action>
    1. Rewrite lib/db.ts to detect the database provider from DATABASE_URL:
       - If DATABASE_URL starts with "postgres://" or "postgresql://", use the @prisma/adapter-pg adapter (import Pool from "pg", create pool with DATABASE_URL, pass to PrismaPg constructor, pass adapter to PrismaClient).
       - If DATABASE_URL starts with "file:" or is absent, use @prisma/adapter-better-sqlite3 adapter (current behavior — import PrismaBetterSqlite3, extract file path, create adapter).
       - Export the same `prisma` singleton. The detection logic goes inside createPrisma(). Keep the globalForPrisma caching pattern for dev hot-reload.
       - Import structure: use dynamic require or conditional at module top since both adapters are installed. Use a simple if/else on the URL prefix string.

    2. In prisma.config.ts, no changes needed — it already reads DATABASE_URL from env and falls back to "file:./dev.db". This is correct for both SQLite local and Postgres production.

    3. Generate the initial Postgres migration. Run: DATABASE_URL="postgresql://placeholder:placeholder@localhost:5432/placeholder" npx prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script > prisma/migrations/0001_init/migration.sql. Create the directory prisma/migrations/0001_init/ first. This creates the SQL migration file without needing a real Postgres database.

    4. Verify the app still builds by running: npm run build. If there are type errors from the dual adapter, fix them. The PrismaClient constructor accepts { adapter } in Prisma 7 for both adapters.
  </action>
  <verify>
    <automated>cd /Users/victorroque/Documents/Claude-Code/shipoffers-tracking-report && grep -v '^//' lib/db.ts | grep -c 'adapter-pg' && grep -v '^//' lib/db.ts | grep -c 'adapter-better-sqlite3' && echo "Both adapters referenced" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>lib/db.ts conditionally selects pg or sqlite adapter based on DATABASE_URL prefix. Migration SQL file exists at prisma/migrations/0001_init/migration.sql. npm run build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DATABASE_URL | Connection string contains credentials, must not be logged or exposed client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | I (Information Disclosure) | lib/db.ts | mitigate | Never log DATABASE_URL value; pg Pool inherits SSL from connection string; ensure no console.log of connection details |
| T-04-02 | T (Tampering) | prisma/migrations | accept | Migration files committed to git — tamper is detectable via git history; Vercel runs migrations from trusted repo |
</threat_model>

<verification>
1. grep confirms postgresql provider in schema.prisma
2. lib/db.ts references both adapter-pg and adapter-better-sqlite3
3. npm run build succeeds
4. Migration SQL file exists
</verification>

<success_criteria>
- Prisma schema uses postgresql provider
- lib/db.ts works with both SQLite (dev) and PostgreSQL (prod) based on DATABASE_URL
- Initial migration SQL is generated
- Application builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-production-infrastructure/04-01-SUMMARY.md`
</output>
