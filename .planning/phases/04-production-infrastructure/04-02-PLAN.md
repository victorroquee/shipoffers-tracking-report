---
phase: 04-production-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - .env.local
  - .gitignore
  - next.config.ts
  - app/api/health/route.ts
autonomous: true
requirements: [INFRA-02, INFRA-03]
must_haves:
  truths:
    - ".env.example lists every environment variable the app reads"
    - ".env.example has descriptive comments for each variable"
    - ".env.example is committed to git (not blocked by .gitignore)"
    - ".env.local uses correct variable names matching what the code reads"
    - "CRON_SECRET, DASHBOARD_USER, DASHBOARD_PASS have placeholder values indicating they must be changed"
    - "vercel.json has cron schedules for sync, track, and alerts endpoints"
    - "next.config.ts is compatible with Vercel deployment"
  artifacts:
    - path: ".env.example"
      provides: "Documented env var template for all required variables"
      min_lines: 20
    - path: ".gitignore"
      provides: "Exception for .env.example"
      contains: "!.env.example"
    - path: "app/api/health/route.ts"
      provides: "Health check endpoint for deploy verification"
      exports: ["GET"]
  key_links:
    - from: ".env.example"
      to: "lib/shipoffers.ts"
      via: "SHIPOFFERS_API_USER, SHIPOFFERS_API_PASS, SHIPOFFERS_STORE_ID variables"
      pattern: "SHIPOFFERS_"
    - from: ".env.example"
      to: "lib/mailer.ts"
      via: "SMTP_* and ALERT_* variables"
      pattern: "SMTP_|ALERT_"
---

<objective>
Document all environment variables in .env.example, fix .env.local to match actual code variable names, ensure Vercel deploy configuration is correct, and add a health check endpoint.

Purpose: INFRA-02 requires all env vars documented for production. INFRA-03 requires security credentials documented with instructions to set secure values. The current .env.local has stale variable names (SHIPOFFERS_API_KEY instead of SHIPOFFERS_API_USER/PASS) and insecure defaults. This plan creates a comprehensive .env.example and ensures the Vercel config is deploy-ready.

Output: .env.example committed to repo, .env.local corrected, .gitignore updated, health check endpoint.
</objective>

<execution_context>
@/Users/victorroque/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victorroque/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-production-infrastructure/04-CONTEXT.md

@.env.local
@.gitignore
@vercel.json
@next.config.ts
@lib/shipoffers.ts
@lib/mailer.ts

<interfaces>
<!-- Env vars actually read by the codebase (from grep analysis) -->
From lib/shipoffers.ts:
  process.env.SHIPOFFERS_API_USER
  process.env.SHIPOFFERS_API_PASS
  process.env.SHIPOFFERS_STORE_ID
  process.env.USE_MOCK

From lib/tracking.ts:
  process.env.SEVENTEENTRACK_API_KEY

From lib/mailer.ts:
  process.env.SMTP_HOST
  process.env.SMTP_PORT
  process.env.SMTP_USER
  process.env.SMTP_PASS
  process.env.ALERT_FROM
  process.env.ALERT_TO_SHIPOFFERS

From app/api/sync/route.ts:
  process.env.CRON_SECRET

From lib/db.ts:
  process.env.DATABASE_URL

<!-- Note: .env.local has SHIPOFFERS_API_KEY which is STALE — code uses SHIPOFFERS_API_USER/PASS -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .env.example with all documented variables and fix .gitignore</name>
  <files>.env.example, .gitignore, .env.local</files>
  <read_first>
    - .env.local (current local vars — has stale SHIPOFFERS_API_KEY)
    - .gitignore (has .env* pattern that blocks .env.example)
    - lib/shipoffers.ts (lines 1-25 for env var names)
    - lib/mailer.ts (for SMTP env var names)
    - app/api/sync/route.ts (line 7 for CRON_SECRET)
  </read_first>
  <action>
    1. Create .env.example with ALL environment variables the application reads, organized in sections with comments. Per D-04 and D-05, include these variables:

       Section "Database":
       - DATABASE_URL: "postgresql://user:password@host:5432/dbname" for production, "file:./dev.db" for local

       Section "Shipoffers API" (per D-06, mark as pending):
       - SHIPOFFERS_API_USER: placeholder "your_shipoffers_username" with comment "# Pending from Ben Schulz tech team"
       - SHIPOFFERS_API_PASS: placeholder "your_shipoffers_password"
       - SHIPOFFERS_STORE_ID: placeholder "your_store_id"
       - USE_MOCK: "true" with comment "# Set to false when Shipoffers credentials are configured"

       Section "17track API":
       - SEVENTEENTRACK_API_KEY: placeholder "your_17track_api_key"

       Section "Email / SMTP":
       - SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS, ALERT_FROM, ALERT_TO_SHIPOFFERS with descriptive placeholders

       Section "Dashboard Auth" (per INFRA-03):
       - DASHBOARD_USER: placeholder "CHANGE_ME" with comment "# REQUIRED: Set a secure username for production"
       - DASHBOARD_PASS: placeholder "CHANGE_ME" with comment "# REQUIRED: Set a secure password for production"

       Section "Cron Security" (per INFRA-03):
       - CRON_SECRET: placeholder "CHANGE_ME_GENERATE_RANDOM_TOKEN" with comment "# REQUIRED: Use a cryptographically random string (e.g., openssl rand -hex 32)"

       Every placeholder value MUST clearly indicate it needs to be replaced. Never use real credentials.

    2. Update .gitignore: add "!.env.example" line right after the ".env*" line so the example file is trackable.

    3. Update .env.local: replace the stale SHIPOFFERS_API_KEY line with SHIPOFFERS_API_USER, SHIPOFFERS_API_PASS, and SHIPOFFERS_STORE_ID (with empty/placeholder values). Add SHIPOFFERS_STORE_ID if missing. Keep all other existing values intact. Ensure USE_MOCK=true remains.
  </action>
  <verify>
    <automated>cd /Users/victorroque/Documents/Claude-Code/shipoffers-tracking-report && test -f .env.example && grep -c 'SHIPOFFERS_API_USER' .env.example && grep -c 'CRON_SECRET' .env.example && grep -c 'DASHBOARD_USER' .env.example && grep -c 'DATABASE_URL' .env.example && grep -q '!.env.example' .gitignore && echo "PASS"</automated>
  </verify>
  <done>.env.example exists with all 15 env vars documented in sections. .gitignore allows .env.example. .env.local uses correct variable names matching the codebase.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Vercel config and add health check endpoint</name>
  <files>next.config.ts, app/api/health/route.ts, vercel.json</files>
  <read_first>
    - vercel.json (current cron config)
    - next.config.ts (current — mostly empty)
  </read_first>
  <action>
    1. Review vercel.json: the current cron config is correct (sync daily at 11:00, track twice daily, alerts 30min after track). No changes needed unless the paths or schedules are wrong.

    2. Review next.config.ts: for Vercel deployment, ensure the config does not set output: "export" or output: "standalone" — default is correct for Vercel. The current empty config is fine for Vercel. No changes needed.

    3. Create app/api/health/route.ts: a simple GET endpoint that returns 200 with JSON body containing status: "ok", timestamp, and environment indicator (production vs development based on NODE_ENV). This is useful for deploy verification and monitoring. Do NOT import prisma or test the database connection — keep it zero-dependency for fast cold starts.

    4. Verify the build succeeds with all changes from this plan: run npm run build. Check that there are no warnings about Vercel-incompatible config.
  </action>
  <verify>
    <automated>cd /Users/victorroque/Documents/Claude-Code/shipoffers-tracking-report && test -f app/api/health/route.ts && curl_output=$(cat app/api/health/route.ts) && echo "$curl_output" | grep -q "GET" && echo "Health route exists" && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>vercel.json has correct cron config. next.config.ts is Vercel-compatible. GET /api/health returns 200 with status ok. npm run build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env.example | Must NOT contain real credentials — only placeholders |
| CRON_SECRET | Protects cron endpoints from unauthorized invocation |
| DASHBOARD_USER/PASS | Protects dashboard from unauthorized access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | I (Information Disclosure) | .env.example | mitigate | Use only placeholder values (CHANGE_ME, your_*); never include real keys; add comments warning to change |
| T-04-04 | S (Spoofing) | CRON_SECRET | mitigate | .env.example instructs user to generate cryptographically random token via openssl rand -hex 32 |
| T-04-05 | S (Spoofing) | DASHBOARD_USER/PASS | mitigate | Placeholder values are "CHANGE_ME" — cannot be used to access dashboard; comments mark as REQUIRED |
| T-04-06 | I (Information Disclosure) | /api/health | accept | Returns only status and timestamp — no sensitive data exposed; useful for uptime monitoring |
</threat_model>

<verification>
1. .env.example contains all 15 required env vars
2. .gitignore has !.env.example exception
3. .env.local uses correct var names (SHIPOFFERS_API_USER not SHIPOFFERS_API_KEY)
4. vercel.json cron config is valid
5. /api/health route exists and exports GET
6. npm run build passes
</verification>

<success_criteria>
- .env.example committed to git with all env vars documented and security placeholders
- .env.local uses variable names that match actual code references
- vercel.json and next.config.ts are Vercel-deploy compatible
- Health check endpoint available at /api/health
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-production-infrastructure/04-02-SUMMARY.md`
</output>
