---
phase: 02-search-filter-pagination
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/page.tsx
autonomous: false
requirements: [SRCH-01, SRCH-02, SRCH-03, DATA-02]

must_haves:
  truths:
    - "User sees a search input above the orders table"
    - "Typing in the search input filters orders by tracking code or customer name after 300ms debounce"
    - "User sees a country dropdown populated with actual countries from the dataset"
    - "Selecting a country filters the table to only that country's orders"
    - "Search + country + status filters all compose simultaneously"
    - "Table shows 25 orders per page with Previous/Next buttons and page indicator"
    - "Changing search, country, or status filter resets pagination to page 1"
    - "Empty state shows 'Nenhum pedido encontrado' when no results match"
  artifacts:
    - path: "app/page.tsx"
      provides: "Dashboard with search, country filter, pagination controls"
      contains: "useState.*search"
  key_links:
    - from: "app/page.tsx"
      to: "/api/orders"
      via: "fetch with search, country, filter, page, per_page query params"
      pattern: "fetch.*api/orders.*search"
    - from: "app/page.tsx"
      to: "/api/orders?countries=true"
      via: "fetch on mount to populate country dropdown"
      pattern: "countries=true"
---

<objective>
Add search input, country dropdown, and pagination controls to the dashboard page. Users can search by tracking code or customer name, filter by country, and navigate paginated results.

Purpose: Make the orders table usable at scale (100-500 orders) with composable filters and pagination.
Output: Updated app/page.tsx with full search/filter/pagination UI.
</objective>

<execution_context>
@/Users/victorroque/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victorroque/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-search-filter-pagination/02-CONTEXT.md
@.planning/phases/02-search-filter-pagination/02-01-SUMMARY.md

<interfaces>
<!-- API response shape from Plan 01 -->

From app/api/orders/route.ts (after Plan 01):
```typescript
// GET /api/orders?search=X&country=Y&filter=Z&page=N&per_page=M
// Response: { orders: Order[], total: number, page: number, per_page: number, total_pages: number }

// GET /api/orders?countries=true
// Response: { countries: string[] }
```

From app/page.tsx (current state):
```typescript
type FilterType = 'all' | 'delayed' | 'delivered'
// Current: fetch(`/api/orders${filter !== 'all' ? `?filter=${filter}` : ''}`)
// Current: setOrders(Array.isArray(data) ? data : [])
// Needs update: response is now { orders, total, page, per_page, total_pages }
```

From components/OrdersTable.tsx:
```typescript
export default function OrdersTable({ orders }: { orders: Order[] })
// No changes needed — receives orders array as prop
// Already has empty state: "Nenhum pedido encontrado"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add search, country dropdown, and pagination to dashboard</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx (full file — current filter bar, load function, state management)
    - .planning/phases/02-search-filter-pagination/02-01-SUMMARY.md (API response shape)
    - components/OrdersTable.tsx (props interface — lines 170-172)
    - node_modules/next/dist/docs/01-app (check for any breaking changes in client components)
  </read_first>
  <action>
    Update app/page.tsx to add search, country filter, and pagination UI (per D-01 through D-13):

    NEW STATE VARIABLES:
    - search: string (controlled input value)
    - debouncedSearch: string (actual value sent to API, debounced 300ms per D-03)
    - country: string (selected country code, empty string = all countries)
    - countries: string[] (list from /api/orders?countries=true)
    - page: number (current page, starts at 1)
    - total: number (total matching orders from API)
    - totalPages: number (total pages from API)

    DEBOUNCE IMPLEMENTATION:
    Use useEffect with setTimeout/clearTimeout pattern for 300ms debounce (per D-03). When search state changes, set a 300ms timeout to update debouncedSearch. Cleanup clears timeout on re-render.

    FETCH COUNTRIES ON MOUNT:
    useEffect on mount: fetch('/api/orders?countries=true'), parse { countries }, set countries state. No dependency on filters.

    UPDATE load() FUNCTION:
    Build query string from: debouncedSearch (as search param), country, filter, page, per_page=25.
    Parse response as { orders, total, page, per_page, total_pages } (not flat array).
    Set orders from data.orders, total from data.total, totalPages from data.total_pages.
    Dependencies: [debouncedSearch, country, filter, page]

    RESET PAGE ON FILTER CHANGE (per D-10):
    When debouncedSearch, country, or filter changes, reset page to 1.
    Use a useEffect watching [debouncedSearch, country, filter] that calls setPage(1).

    UI LAYOUT — FILTER BAR (per D-06, D-11, D-12):
    Below StatsBar, above the status filter buttons, add a horizontal row with:

    1. Search input (per D-01):
       - Placeholder: "Buscar por rastreio ou cliente..." (pt-BR per D-13)
       - Import Search icon from lucide-react
       - Inline CSS matching existing aesthetic: background #FFFFFF, border 1px solid #E5E8EE, borderRadius 8px, padding 7px 12px 7px 32px (room for icon), fontSize 12px, color #0C0E13, width about 280px
       - Search icon positioned absolutely inside the input container, left 10px, color #9299A8

    2. Country dropdown (per D-05, D-06):
       - Default option: "Todos os paises" (value="")
       - Options from countries state array
       - Same inline CSS aesthetic: background #FFFFFF, border 1px solid #E5E8EE, borderRadius 8px, padding 7px 12px, fontSize 12px, color #0C0E13, fontFamily inherit, appearance none with custom chevron via background SVG or lucide icon
       - Sits next to search input with gap 8px

    3. Existing status filter buttons (Todos/Em Atraso/Entregues) remain in same row or below (per D-06)

    UI LAYOUT — PAGINATION CONTROLS (per D-09):
    Below OrdersTable, add a row with:
    - "Mostrando X-Y de Z pedidos" text on the left (discretion item — include it)
    - "Pagina X de Y" in center
    - Previous button (disabled when page===1) and Next button (disabled when page===totalPages)
    - Inline CSS: buttons match existing filter button style, fontSize 12px, gap 8px
    - pt-BR labels: "Anterior" and "Proximo" (per D-13)

    TOTAL COUNT DISPLAY:
    Update the existing orders count display (currently shows `${orders.length} pedidos`) to show `${total} pedidos` from API total, so it reflects the full filtered count not just the page.

    IMPORTANT: Do NOT change components/OrdersTable.tsx — it already handles empty state per D-04 ("Nenhum pedido encontrado"). Just pass the filtered/paginated orders array from API response.
  </action>
  <verify>
    <automated>cd /Users/victorroque/Documents/Claude-Code/shipoffers-tracking-report && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - Search input is visible above the table with placeholder "Buscar por rastreio ou cliente..."
    - Country dropdown shows "Todos os paises" default plus all distinct countries from data
    - Status filter buttons (Todos/Em Atraso/Entregues) still present and functional
    - Pagination controls show below table with Anterior/Proximo buttons and "Pagina X de Y"
    - Typing in search triggers API call after 300ms debounce
    - Selecting a country triggers immediate API call
    - Changing any filter resets to page 1
    - All filters compose: search + country + status + pagination
    - tsc --noEmit passes with zero errors
  </acceptance_criteria>
  <done>Dashboard has search input, country dropdown, and pagination controls. All filters compose. Pagination resets on filter change. UI uses inline CSS matching existing aesthetic. Labels in pt-BR.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of search, filter, and pagination</name>
  <what-built>Search input, country dropdown filter, and pagination controls on the orders dashboard. All filters compose (search + country + status + pagination).</what-built>
  <how-to-verify>
    1. Run: npm run dev (or it may already be running)
    2. Open http://localhost:3000
    3. Verify the search input appears with placeholder "Buscar por rastreio ou cliente..."
    4. Type "hans" — after ~300ms, table should filter to orders matching "hans" in customer name
    5. Clear search, type "LX123" — should filter to orders matching that tracking code
    6. Clear search, use country dropdown — select "DE", only German orders appear
    7. With "DE" selected, also click "Em Atraso" — both filters apply simultaneously
    8. Clear all filters — verify pagination shows "Pagina 1 de 2" with 30 mock orders at 25/page
    9. Click "Proximo" — page 2 shows remaining 5 orders
    10. Click "Anterior" — back to page 1
    11. Type a search that matches nothing — empty state "Nenhum pedido encontrado" appears
    12. Verify visual design matches existing dark header / light content aesthetic
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> search param | Free text from search input sent to API |
| user input -> country param | Dropdown value sent to API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T (Tampering) | search input | accept | Input is passed as URL query param to own API. API uses Prisma parameterized queries. No XSS risk since React escapes output. Low risk for internal tool. |
| T-02-05 | S (Spoofing) | country dropdown | accept | Dropdown values come from API. Even if tampered, API validates against actual data via Prisma where clause. |
</threat_model>

<verification>
1. npm run dev
2. Open http://localhost:3000
3. Test search by name ("hans"), by tracking code ("LX123")
4. Test country filter dropdown
5. Test composing search + country + status
6. Test pagination navigation
7. Test filter change resets to page 1
8. tsc --noEmit passes
</verification>

<success_criteria>
User can search by tracking code or customer name, filter by country, and paginate through results. All filters compose. Pagination resets on filter change. UI matches existing aesthetic with pt-BR labels.
</success_criteria>

<output>
After completion, create `.planning/phases/02-search-filter-pagination/02-02-SUMMARY.md`
</output>
