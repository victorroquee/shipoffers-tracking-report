---
phase: 02-search-filter-pagination
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/orders/route.ts
  - lib/mock-data.ts
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-03, DATA-02]

must_haves:
  truths:
    - "GET /api/orders?search=hans returns only orders matching 'hans' in customerName or trackingCode"
    - "GET /api/orders?country=DE returns only orders with destinationCountry='DE'"
    - "GET /api/orders?page=1&per_page=25 returns first 25 orders plus total count"
    - "Search, country, filter, and pagination compose simultaneously"
    - "Mock mode applies same search/country/pagination logic client-side"
  artifacts:
    - path: "app/api/orders/route.ts"
      provides: "Orders API with search, country filter, pagination"
      exports: ["GET"]
    - path: "lib/mock-data.ts"
      provides: "Mock data filtering with search/country/pagination support"
      exports: ["getMockDBOrders"]
  key_links:
    - from: "app/api/orders/route.ts"
      to: "prisma.order.findMany"
      via: "where clause with search/country + skip/take"
      pattern: "prisma\\.order\\.(findMany|count)"
    - from: "app/api/orders/route.ts"
      to: "getMockDBOrders"
      via: "mock mode filtering"
      pattern: "getMockDBOrders"
---

<objective>
Add search, country filter, and server-side pagination to the /api/orders endpoint. Both mock mode and Prisma mode support the same query parameters: search (tracking code or customer name), country, filter (status), page, per_page.

Purpose: The API must return paginated, filtered results with total count so the UI can render pagination controls and composable filters.
Output: Updated /api/orders route returning { orders: Order[], total: number, page: number, per_page: number, total_pages: number }.
</objective>

<execution_context>
@/Users/victorroque/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victorroque/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-search-filter-pagination/02-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From prisma/schema.prisma:
```prisma
model Order {
  id                 String       @id @default(cuid())
  shipofffersId      String       @unique
  trackingCode       String?
  customerName       String?
  customerEmail      String?
  destinationCountry String?
  shippedAt          DateTime?
  deliveredAt        DateTime?
  status             String       @default("UNKNOWN")
  lastTrackingSync   DateTime?
  daysInTransit      Int?
  delayThreshold     Int?
  isDelayed          Boolean      @default(false)
  alertSentAt        DateTime?
  rawTrackingData    String?
  createdAt          DateTime     @default(now())
  updatedAt          DateTime     @updatedAt
  events             OrderEvent[]
}
```

From lib/mock-data.ts:
```typescript
export function getMockDBOrders(): MockDBOrder[] // returns array of 30 orders with full Prisma-like shape
```

From app/api/orders/route.ts (current):
```typescript
// Currently accepts only ?filter=delayed|delivered
// Returns: NextResponse.json(orders) — flat array, no pagination metadata
export async function GET(req: Request)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add search, country filter, and pagination to /api/orders</name>
  <files>app/api/orders/route.ts, lib/mock-data.ts</files>
  <read_first>
    - app/api/orders/route.ts (current implementation — 42 lines)
    - lib/mock-data.ts (getMockDBOrders function — lines 164-210)
    - prisma/schema.prisma (Order model fields)
    - node_modules/next/dist/docs/01-app (check route handler patterns for Next.js 16)
  </read_first>
  <action>
    Rewrite app/api/orders/route.ts GET handler to accept these query params (per D-01, D-02, D-05, D-07, D-08, D-10):

    Query params:
    - search (string, optional) — case-insensitive match against trackingCode OR customerName (per D-01, SRCH-01, SRCH-02)
    - country (string, optional) — exact match on destinationCountry (per D-05, SRCH-03)
    - filter (string, optional) — existing "delayed" or "delivered" status filter (preserve current behavior)
    - page (number, default 1) — 1-indexed page number (per D-08, DATA-02)
    - per_page (number, default 25) — items per page, capped at 100 (per D-08)

    Response shape change — return JSON object instead of flat array:
    { orders: Order[], total: number, page: number, per_page: number, total_pages: number }

    For Prisma mode:
    - Build where clause composing all filters with AND logic (per D-07)
    - For search: use OR with contains on trackingCode and customerName. Since SQLite does not support mode:"insensitive", use Prisma raw or lowercase both sides. Simplest: use two contains clauses in OR — SQLite LIKE is case-insensitive by default for ASCII.
    - For country: where.destinationCountry = country
    - For filter: existing logic (isDelayed + status not DELIVERED, or status DELIVERED)
    - Use prisma.order.count(where) for total
    - Use prisma.order.findMany with skip: (page-1)*per_page, take: per_page
    - Keep existing orderBy: [{ isDelayed: 'desc' }, { daysInTransit: 'desc' }]
    - Include events only when filter === 'delayed' (existing behavior)

    For mock mode:
    - Get all mock orders via getMockDBOrders()
    - Apply same filtering in-memory: search (case-insensitive includes on customerName and trackingCode), country (exact match), filter (delayed/delivered)
    - Slice for pagination: orders.slice((page-1)*perPage, page*perPage)
    - Return same { orders, total, page, per_page, total_pages } shape

    Also add to lib/mock-data.ts: export a function getDistinctMockCountries() that returns sorted unique destinationCountry values from MOCK_ORDERS. This will be used by a new /api/orders/countries endpoint or inlined.

    Actually, add a new query param countries=true that, when set, returns { countries: string[] } instead of the orders response. This avoids creating a separate route. The countries list comes from:
    - Mock mode: distinct values from getMockDBOrders()
    - Prisma mode: prisma.order.findMany with distinct on destinationCountry, select destinationCountry only

    Per D-05: country dropdown populated from distinct destinationCountry values in dataset.
  </action>
  <verify>
    <automated>cd /Users/victorroque/Documents/Claude-Code/shipoffers-tracking-report && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - GET /api/orders returns { orders: [...], total: 30, page: 1, per_page: 25, total_pages: 2 } in mock mode
    - GET /api/orders?search=hans returns orders matching "hans" in customerName (case-insensitive)
    - GET /api/orders?search=LX123 returns orders matching "LX123" in trackingCode
    - GET /api/orders?country=DE returns only German orders
    - GET /api/orders?search=hans&country=DE&filter=delayed composes all three filters
    - GET /api/orders?page=2&per_page=25 returns second page with correct total
    - GET /api/orders?countries=true returns { countries: ["AT","BE","BR",...] } sorted
    - tsc --noEmit passes with zero errors
  </acceptance_criteria>
  <done>API returns paginated, filtered results with total count metadata. Search matches tracking code and customer name. Country filter works. All filters compose. Countries list endpoint works. Both mock and Prisma modes supported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/orders | Query params from untrusted browser input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T (Tampering) | search/country params | mitigate | Validate and sanitize: page/per_page parsed as integers with Math.max(1, ...) and Math.min(100, ...). search/country are strings passed to Prisma parameterized queries (no SQL injection). |
| T-02-02 | D (Denial of Service) | per_page param | mitigate | Cap per_page at 100 to prevent memory exhaustion from requesting all rows. |
| T-02-03 | I (Information Disclosure) | error responses | accept | Existing pattern: generic error message to client, full error to console.error. Low risk for internal tool. |
</threat_model>

<verification>
curl "http://localhost:3000/api/orders?search=hans" — returns filtered results
curl "http://localhost:3000/api/orders?country=DE" — returns DE orders only
curl "http://localhost:3000/api/orders?page=2&per_page=10" — returns page 2
curl "http://localhost:3000/api/orders?countries=true" — returns country list
</verification>

<success_criteria>
/api/orders accepts search, country, filter, page, per_page params and returns { orders, total, page, per_page, total_pages }. TypeScript compiles. Both mock and Prisma modes work identically.
</success_criteria>

<output>
After completion, create `.planning/phases/02-search-filter-pagination/02-01-SUMMARY.md`
</output>
