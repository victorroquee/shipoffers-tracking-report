---
phase: 03-export-configuration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - lib/delay-rules.ts
  - app/api/settings/thresholds/route.ts
autonomous: true
requirements: [DATA-03]

must_haves:
  truths:
    - "DelayThreshold model exists in Prisma schema with countryCode, countryName, days fields"
    - "GET /api/settings/thresholds returns all thresholds as JSON array"
    - "PUT /api/settings/thresholds updates a single threshold by countryCode"
    - "getDelayThreshold() reads from database first, falls back to hardcoded defaults"
    - "Database is seeded with all 20 countries from current DELAY_RULES"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "DelayThreshold model"
      contains: "model DelayThreshold"
    - path: "app/api/settings/thresholds/route.ts"
      provides: "Threshold CRUD API"
      exports: ["GET", "PUT"]
    - path: "lib/delay-rules.ts"
      provides: "DB-first threshold lookup with hardcoded fallback"
      contains: "prisma.delayThreshold"
  key_links:
    - from: "app/api/settings/thresholds/route.ts"
      to: "prisma.delayThreshold"
      via: "Prisma queries"
      pattern: "prisma\\.delayThreshold\\.(findMany|update|upsert)"
    - from: "lib/delay-rules.ts"
      to: "prisma.delayThreshold"
      via: "findUnique with fallback"
      pattern: "prisma\\.delayThreshold\\.findUnique"
---

<objective>
Create the DelayThreshold Prisma model, seed it with existing hardcoded values, build the /api/settings/thresholds API route (GET all, PUT update), and update getDelayThreshold() to read from DB first with hardcoded fallback.

Purpose: Backend foundation for threshold management — operators will be able to persist and modify delay thresholds via API, and the alert system will read from the database instead of hardcoded values.
Output: Prisma model + migration, API route, updated delay-rules.ts
</objective>

<execution_context>
@/Users/victorroque/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victorroque/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-export-configuration/03-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From prisma/schema.prisma (current — no DelayThreshold yet):
```prisma
datasource db {
  provider = "sqlite"
}
model Order {
  id String @id @default(cuid())
  ...
  delayThreshold Int?
  isDelayed Boolean @default(false)
  ...
}
```

From lib/delay-rules.ts:
```typescript
const DELAY_RULES: Record<string, number> = {
  DE: 7, AT: 7, CH: 7, NL: 7, BE: 7,
  FR: 10, PT: 10, SE: 10, DK: 10, GB: 10,
  FI: 12, IT: 12, ES: 12,
  PL: 14, CZ: 14, HU: 14, NO: 14, US: 14,
  RO: 18,
  BR: 21,
  DEFAULT: 14,
}
export function getDelayThreshold(countryCode: string | null | undefined): number
```

From lib/db.ts:
```typescript
export const prisma: PrismaClient
```

From app/api/orders/route.ts (pattern reference):
```typescript
import { NextResponse } from 'next/server'
import { prisma } from '@/lib/db'
const isMock = process.env.USE_MOCK === 'true'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DelayThreshold model to Prisma and seed with current values</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (full file — already in interfaces above)
    - lib/delay-rules.ts (DELAY_RULES object — seed source)
  </read_first>
  <action>
Add a new model `DelayThreshold` to prisma/schema.prisma per D-08. Fields: `id` (String, @id @default(cuid())), `countryCode` (String, @unique), `countryName` (String), `days` (Int), `updatedAt` (DateTime @updatedAt). Place it after the OrderEvent model.

After adding the model, run `npx prisma db push` to apply the schema change to the SQLite dev database (this project uses db push, not migrations, for SQLite dev).

Then seed the database with all 20 countries from DELAY_RULES. Use a Node script via `npx tsx` or inline `node -e` that imports prisma from lib/db and runs `prisma.delayThreshold.upsert()` for each country. The country name mapping: DE=Alemanha, AT=Austria, CH=Suica, NL=Holanda, BE=Belgica, FR=Franca, PT=Portugal, SE=Suecia, DK=Dinamarca, GB=Reino Unido, FI=Finlandia, IT=Italia, ES=Espanha, PL=Polonia, CZ=Republica Tcheca, HU=Hungria, NO=Noruega, US=EUA, RO=Romenia, BR=Brasil. Do NOT seed DEFAULT — it is a fallback, not a country.

Use upsert (create if not exists, update if exists) so the seed is idempotent.
  </action>
  <verify>
    <automated>npx prisma db push --accept-data-loss 2>&1 | grep -q "applied" || echo "schema pushed"; node -e "const{prisma}=require('./lib/db');prisma.delayThreshold.count().then(c=>{console.log('count:',c);process.exit(c===20?0:1)}).catch(()=>process.exit(1))"</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains `model DelayThreshold` with countryCode (@unique), countryName, days fields
    - Database has exactly 20 DelayThreshold rows matching DELAY_RULES values
    - Prisma client can query delayThreshold without errors
  </acceptance_criteria>
  <done>DelayThreshold model exists in schema, database seeded with 20 countries, prisma.delayThreshold.count() returns 20</done>
</task>

<task type="auto">
  <name>Task 2: Create threshold API route and update getDelayThreshold to read from DB</name>
  <files>app/api/settings/thresholds/route.ts, lib/delay-rules.ts</files>
  <read_first>
    - app/api/orders/route.ts (pattern for API route structure, NextResponse usage, isMock pattern)
    - lib/delay-rules.ts (current getDelayThreshold signature and DELAY_RULES)
    - lib/db.ts (prisma import path)
  </read_first>
  <action>
**Create `app/api/settings/thresholds/route.ts`** per D-10:

GET handler: Query `prisma.delayThreshold.findMany({ orderBy: { countryName: 'asc' } })`. Return JSON array of all thresholds. In mock mode (USE_MOCK=true), return the DELAY_RULES object transformed into the same shape (array of {countryCode, countryName, days}).

PUT handler: Accept JSON body `{ countryCode: string, days: number }`. Validate that `days` is a positive integer (1-365 range). Use `prisma.delayThreshold.update({ where: { countryCode }, data: { days } })`. Return the updated record. If countryCode not found, return 404 with `{ error: 'Pais nao encontrado' }`. In mock mode, return 501 with `{ error: 'Configuracao nao disponivel em modo mock' }`.

**Update `lib/delay-rules.ts`** per D-11:

Keep the existing `DELAY_RULES` object and its `DEFAULT` key as the fallback. Modify `getDelayThreshold()` to be async: `export async function getDelayThreshold(countryCode: string | null | undefined): Promise<number>`. Inside, try `prisma.delayThreshold.findUnique({ where: { countryCode: code } })` — if found, return `record.days`. If not found or any error (DB unavailable, mock mode), fall back to `DELAY_RULES[code] ?? DELAY_RULES.DEFAULT`.

Import prisma from `@/lib/db` at the top of delay-rules.ts.

Also export a synchronous `getDelayThresholdSync()` that preserves the original behavior (reads only from DELAY_RULES) for any callers that cannot await. This avoids breaking existing sync callers during this phase.

IMPORTANT: Search the codebase for all current callers of `getDelayThreshold` before modifying the signature. If any caller is synchronous and cannot easily be made async, use `getDelayThresholdSync` there instead.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20; curl -s http://localhost:3000/api/settings/thresholds 2>/dev/null | node -e "let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{const a=JSON.parse(d);console.log('count:',a.length);process.exit(a.length>=20?0:1)})" 2>/dev/null || echo "Server not running — tsc check is sufficient"</automated>
  </verify>
  <acceptance_criteria>
    - app/api/settings/thresholds/route.ts exports GET and PUT handlers
    - GET returns JSON array with 20 threshold objects (countryCode, countryName, days)
    - PUT with valid {countryCode, days} updates the record and returns it
    - PUT with invalid countryCode returns 404
    - PUT with days outside 1-365 returns 400
    - lib/delay-rules.ts exports async getDelayThreshold that reads DB first, falls back to DELAY_RULES
    - lib/delay-rules.ts exports getDelayThresholdSync for backward compatibility
    - tsc --noEmit passes with zero errors
  </acceptance_criteria>
  <done>Threshold API returns 20 countries on GET, accepts PUT updates, getDelayThreshold reads from DB with fallback, TypeScript compiles cleanly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> PUT /api/settings/thresholds | Untrusted JSON body with countryCode and days values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | PUT /api/settings/thresholds | mitigate | Validate days is integer in 1-365 range; validate countryCode exists in DB before update |
| T-03-02 | Spoofing | PUT /api/settings/thresholds | accept | No auth on settings endpoint — internal tool, same auth as dashboard (Basic Auth from Phase 1) |
| T-03-03 | Info Disclosure | GET /api/settings/thresholds | accept | Threshold data is not sensitive — country codes and day counts only |
| T-03-04 | Denial of Service | PUT /api/settings/thresholds | accept | Internal tool with limited users; no rate limiting needed |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `prisma.delayThreshold.count()` returns 20
3. GET /api/settings/thresholds returns 20 threshold objects
4. PUT /api/settings/thresholds with valid payload updates and returns the record
</verification>

<success_criteria>
- DelayThreshold model in Prisma schema with 20 seeded rows
- API route serves GET (list all) and PUT (update one) for thresholds
- getDelayThreshold reads from DB first, falls back to hardcoded defaults
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-export-configuration/03-01-SUMMARY.md`
</output>
